---
description:
globs:
alwaysApply: false
---
需要仔细阅读这个接口文档说明
目录结构中，1级子目录是api的接口名字，每个接口分开放
code目录下是放代码，output是放请求的返回，params所有的业务逻辑调用参数
项目中一般会有类似的接口实现和功能，可以进行参考。
这些并发和参数什么的，都放到这个params目录中，我可以调整和直观看到代码中所有的参数设置。config.json中保存
有些接口的调用，需要并发才可以完成，设置7个并发。
还要设计失败机制，获取了一堆接口中，有少量的失败，需要后续补充单独失败的，而不要重复调用所有的。
测试先调用少量几次接口，取少量页的数据，给用户确认接口的调用情况返回内容。确认翻页能力和数据情况。
记得要把请求题和返回体完整的都记录下来。
测试完成后，需要把整体数据的情况说一下，全部调用完需要多少次？
这些代码运行的进展怎么样需要展现出来，这个程序执行了，如果要很久的话，中间需要给一个进度，什么的，否则这个是直接卡死还是什么的，完全不知道。跑一个api就落文件，然后把进度就打印出来。
你需要理解业务需求，阅读接口说明，然后写代码，运行一次给我看一下结果，分析一下返回体的结构内容。
需要从业务角度分析一下返回内容，而不是单纯的调用完成接口，确保返回的业务意义。
因为每次接口调用都需要付费，所以要比较非常谨慎的调用，不要发生大量的重复调用。调用过就立刻保存下来，不要等整体job完成才保存，万一有失败和报错，就等于白调用了。